#include<bits/stdc++.h>
using namespace std;

#define lli long long int
#define pb push_back
#define f first
#define sec second

main()
{
    lli n,i,a,t,b,c,j,k;

    cin>>n;
    string s;
    lli arr[n];

    cin>>s;

    for(i=0;i<n;i++)
    {
        if(s[i] == '(')
        {
            arr[i] = 1;
        }
        else
        {
            arr[i] = -1;
        }
    }





}
