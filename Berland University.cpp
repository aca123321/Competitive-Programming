#include<bits/stdc++.h>
using namespace std;

#define lli long long int


main()
{
    lli t,n,a,b,k,num,max,st=0,rem=0;

    cin>>t>>n>>a>>b>>k;

    min = (a<=b)?(a):(b);

    st += (n*min)/k;
    rem += (n*min)%k;

    if()
}
