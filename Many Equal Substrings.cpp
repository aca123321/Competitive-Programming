#include<iostream>
#include<string>
using namespace std;

main()
{
    int n,k,i,j,count=0,ind,a,b,c;
    string s,t,x;

    cin>>n>>k;
    cin>>t;

    for(i=0;i<n;i++)
    {
        string y,x;

        for(j=0;j<=i;j++)
        {
            y[j] = t[j];
        }

        for(j=i+1;j<n;j++)
        {
            x[j] = t[j];
        }

        a = x.find(y);

        if((i+a))
    }
}
