#include<bits/stdc++.h>
using namespace std;

#define lli long long INT_MAX

main()
{
    lli n,i,t;
    string s;
    lli arr[26];
    cin>>t;

    while(t--)
    {
        cin>>s;
        n = s.length();

        for(i=0;i<26;i++)
        {
            arr[i] = 0;
        }

        for(i=0;i<n;i++)
        {

        }
    }
}
