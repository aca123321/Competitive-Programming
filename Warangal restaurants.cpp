#include<iostream>
using namespace std;

struct rest
{
    long long int x,y;
};

main()
{
    int t,l;
    long long int n,i,j,*ans,count;
    struct rest *r;


    cin>>t;
    ans = new long long int[t];

    for(l=0;l<t;l++)
    {
        cin>>n;

        count = 0;

        r = new rest[n]

        for(j=0;j<n;j++)
        {
            cin>>r[j].x;
            cin>>r[j].y
        }

        for(i=0;i<n;i++)
        {
            for(j=)
        }
    }
}
