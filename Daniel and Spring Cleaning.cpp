#include<bits/stdc++.h>
using namespace std;

#define lli long long int
#define pb push_back
#define f first
#define sec second

lli f(lli x)
{
    lli count = 0;
    while(x != 0)
    {
        if (!(x & 1))
        {
            count++;
        }
        x >>= 1;
    }
    return count;
}

main()
{
    lli n,i,a,t,j,k,l,r;

    cin>>t;

    while(t--)
    {
        cin>>l>>r;

        for(i=l;i<=)
    }

}

