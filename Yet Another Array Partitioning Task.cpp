#include<bits/stdc++.h>
using namespace std;

#define lli long long int

main()
{
    lli i,n,m,a,b,k,sum=0,dud;

    cin>>n>>m>>k;
    lli arr[n],brr[n],;

    for(i=0;i<n;i++)
    {
        cin>>arr[i];
        brr[i] = arr[i];
    }

    sort(brr,brr+n,greater<int>());
    dud = brr[(m*k)-1];

    lli j = 0;

    a = 0;

    while(i<n)
    {
        if()
    }

}
