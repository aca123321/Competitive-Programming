#include<bits/stdc++.h>
using namespace std;

#define lli long long int
#define pb push_back

main()
{
    lli n,i,a,b,c,key,j,k;
    cout<<"Enter the key:\n";
    cin>>key;
    fflush(stdin);
    cout<<"Enter the string to be decoded\n";
    string s,t;
    fflush(stdout);
    getline(cin,s);
    fflush(stdin);
    n = s.length();
    vector<char> v[key];
    t = "";
    a = 0;


}
